name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: macos-13

    steps:
    - uses: actions/checkout@v4
    - run: uname -a
    - run: pwd
    - run: ls
    - run: brew install autoconf automake autoconf-archive libtool
    - run: mkdir build && cd build
    - run: ls
    - run: ls ..
    - run: cmake ..
    - run: make
    - run: ./simple_executable
    - run: vcpkg install boost:arm64-osx
